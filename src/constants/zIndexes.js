export const EVENTS_BG = 0;

export const GROUP_HEADER_EVENT_CELL = 0;

export const TODAY_MARK_LINE = 1;

export const CANVAS_RIGHT = 1;

export const EVENT_CELL = 2;

// the content in toolbar(3) need cover event cell(2).
export const TOOLBAR = 3;

export const TOOLBAR_BLANK_ZONE = 4;

export const TIMELINE_SETTING = 3;

// the toolbar is 3, then the left pane wrapper need to be 4.
export const LEFT_PANE_WRAPPER = 4;

export const TIMELINE_DIALOG = 1048;

// Timeline dialog is 1048, then dropdown menu is 1049.
export const DROPDOWN_MENU = 1049;

// Timeline dialog is 1048, then rc-calendar container is 1049.
export const RC_CALENDAR = 1049;